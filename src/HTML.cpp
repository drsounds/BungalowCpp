#include "HTML.h"
#include <regex>
namespace spider {
HTML::HTML(string *text)
{
    //ctor
    /*this->text = text;
    this->uri = "";
    this->position = 0;
    std::regex rgx("\\<(a|link) (href|uri)\\=\"([a-zA-Z0-9\\:\\/]+)\"\\>(.*)\\<\\/(a|link)\\>");
    std::regex_token_iterator iter(string_to_split.begin(),
    string_to_split.end(),
    rgx,
    -1);*/


}

HTML::~HTML()
{
    //delete this->uri;
    //delete this->innerText;
    //dtor
}
}
